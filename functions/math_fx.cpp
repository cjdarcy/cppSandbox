#include <iostream>
#include <cmath>
using namespace std;

int main () {
    double log_1 = log(17.0);
    double angle = 1.5;
    double height = sin(angle);

    double pi = acos(-1.0);
    double degrees = 90;
    double angle_2 = degrees * 2 * pi / 360.0;

    double x = cos(angle + pi / 2);
    double x_1 = exp(log(10.0));
}
